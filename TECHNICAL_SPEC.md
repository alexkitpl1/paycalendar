# PayCalendar — Полное техническое задание
> Составлено по итогам разработки. Версия 2.0 — апрель 2026.  
> Объединяет задачи из чата, результаты аудита кода и статус выполнения.

**Проект:** PayCalendar  
**URL:** https://paycalendar-production.up.railway.app  
**GitHub:** https://github.com/alexkitpl1/paycalendar  
**Период разработки:** 2–5 апреля 2026  
**Стек:** Python Flask + Railway Volume + Google Drive + Synology WebDAV

---

## СТАТУСЫ

| Символ | Значение |
|--------|----------|
| ✅ | Реализовано и проверено |
| 🔧 | Реализовано, требует проверки на продакшене |
| ❌ | Не реализовано (задача на будущее) |

---

## ФАЗА 1 — Windows агент (02.04.2026)

### 1.1 Первоначальная задача ✅
**Запрос:** Создать приложение которое сканирует почту и находит счета, отображает в виде календаря.

- ✅ Python скрипт сканирования zone.eu IMAP
- ✅ Парсинг писем на предмет счетов (arve, invoice, счёт)
- ✅ Отображение найденных счетов
- ✅ Запуск через `.bat` файл на Windows

### 1.2 Windows portable сборка ✅
**Запрос:** Сделать portable версию без установки Python.

- ✅ Сборка с embedded Python
- ✅ `run.bat` для запуска
- ✅ Автоустановка зависимостей

### 1.3 Веб-интерфейс ✅
**Запрос:** Сделать веб-интерфейс с календарём вместо консоли.

- ✅ Flask веб-сервер
- ✅ HTML страница с календарём
- ✅ Автооткрытие браузера при запуске

---

## ФАЗА 2 — Облако Railway (03.04.2026)

### 2.1 Деплой на Railway ✅
- ✅ Railway деплой через GitHub автосинк
- ✅ `Procfile` с gunicorn
- ✅ Переменные окружения для почты

### 2.2 IMAP сканирование ✅
**Запрос:** Сканировать zone.eu почту через IMAP напрямую.

- ✅ `imaplib` подключение к `imap.zone.eu:993`
- ✅ Стратегия `ALL` писем (обходит case-sensitive SUBJECT)
- ✅ Лимит 2000 последних писем
- ✅ Фоновый скан в отдельном потоке
- ✅ Content-Type из заголовка письма для определения вложений (без загрузки тела)
- 🔧 Прогресс каждые 200 заголовков: `emit("📥 Заголовки: 200/3918...", "info")`

### 2.3 AI анализ писем ✅
**Запрос:** Использовать AI для определения является ли письмо счётом.

- ✅ Gemini API (первичный)
- ✅ Groq API: `llama-3.1-8b-instant` — в 3-4 раза быстрее чем 70b, `max_tokens:400`
- ✅ OpenAI API (fallback)
- ✅ Claude API (fallback)
- ✅ HuggingFace бесплатные модели (fallback)
- ✅ Keyword анализ (всегда работает)
- ✅ Fast-path: если `score >= 75` и есть вложение — пропускаем AI

### 2.4 Ротация AI ключей ✅
**Запрос:** Все ключи нужно использовать — если один закончился второй автоматически.

- ✅ `KeyPool` класс с автоматической ротацией
- ✅ Цепочка: Gemini key1→key2→key3 → Groq key1→key2 → OpenAI → Claude → HuggingFace → keywords
- ✅ `_active_provider()` проверяет живые пулы, не глобальные переменные
- ✅ Ключи сохраняются в `/data/config.ini` на Railway Volume
- ✅ Workers: `min(n_keys * 2, 20)` — до 20 параллельных потоков

### 2.5 Страница скана ✅
**Запрос:** Отдельная страница для запуска скана с прогресс-баром.

- ✅ `/rescan` — страница с шагами: IMAP → Сброс → Скан
- ✅ Живой лог с временными метками
- ✅ Счётчики: найдено счетов / писем в ящике / время

---

## ФАЗА 3 — UI и функциональность (03–04.04.2026)

### 3.1 Дизайн календаря ✅
**Запрос:** Переделать дизайн, тёмная тема, мобильная версия.

- ✅ Тёмная тема (переменные CSS)
- ✅ Светлая тема (переключатель)
- ✅ Серая тема
- ✅ Адаптив для телефона (PWA)
- ✅ Установка на главный экран (Add to Home Screen)
- ✅ Тема сохраняется: `setTheme()` → `POST /api/save-config` → `config.ini` на Volume
- ✅ При загрузке: `GET /api/config` → `cfg.ui_theme` → `setTheme()`

### 3.2 Вкладки интерфейса ✅
**Запрос:** Добавить вкладки: Месяц, Неделя, Долги, Офферы, Контракты.

- ✅ Месяц — стандартный календарь
- ✅ Неделя — недельный вид
- ✅ ⚠ Долги — просроченные счета
- ✅ 💼 Офферы — коммерческие предложения
- ✅ 📋 Контракты — договора отдельно от счетов

### 3.3 Карточки Долги — сетка ✅
**Запрос:** Одинаковые размеры, перенос текста, несколько строк, кнопки переключения вид ⊞/☰, заголовок закреплён.

- ✅ CSS Grid: `repeat(4,1fr)` на десктопе, 3/2/1 на меньших экранах (`.inv-grid`)
- ✅ 4 карточки в строку на десктопе, 1 на мобильном
- ✅ Sticky-заголовок: `position:sticky; top:0` с суммой и тоглом ⊞/☰
- ✅ Карточки: `display:flex; flex-direction:column` + `flex:1` спейсер
- ✅ `-webkit-line-clamp:2` для вендора и описания
- ✅ Кнопка ✓ Оплачен → `markPaidOv(id)` — обновляет без перезагрузки (исправлен баг: функция отсутствовала)
- ✅ Нумерация #01 #02...
- ✅ Дата "до ГГГГ-ММ-ДД"
- ✅ Сортировка: приоритет → дата

### 3.4 Офферы — сетка ✅
**Запрос:** Офферы располагай такими же квадратиками, как просрочено.

- ✅ `renderOffers()` использует тот же `.inv-grid`
- ✅ Sticky-заголовок: `💼 Офферы · N шт. · сумма`
- ✅ 4 карточки в строку на десктопе, 1 на мобильном
- ✅ Карточки с фиолетовой полоской (`border-top: 3px solid #8b5cf6`)
- ✅ Hover-эффект тени

### 3.5 Статус-бар (sum bar) ✅
**Запрос:** ИТОГО МЕСЯЦ / К оплате / НЕДЕЛЯ при нажатии должны открывать список счетов. Каждое поле нажимаемое.

- ✅ Все `.sb` блоки имеют `onclick`
- ✅ `window._sb` хранит предвычисленные наборы: `mAll, mPend, mPaid, mOver, mEe, wAll, wPend, wPaid`
- ✅ `openInvoiceList(title, invoices, canPay)` — универсальная модальная таблица с кнопкой ✓ Оплачен
- ✅ Блок К оплате → список неоплаченных с кнопками оплаты
- ✅ Блок Просрочено → переход во вкладку Долги (`setView('overdue')`)
- ✅ Все блоки подсвечиваются при наведении
- ✅ Пилюли: `fmtShort(n)` — сокращение сумм `1.2 M€`, `540 k€`
- ✅ `flex-wrap: wrap` на контейнере пилюль

### 3.6 Фильтрация контрактов ✅
**Требование:** Контракты/договоры только во вкладке 📋 Контракты, не в счетах.

- ✅ `isContract(inv)` проверяет поле `is_contract` и список ключевых слов `CONTRACT_KW`
- ✅ Список КС: `contract, agreement, leping, договор, контракт, vereinbarung, raamleping, kokkulepe, соглашение, t2ic_office, освидетельств, well technology, контракт №, 01-01-03-, неучастии`
- ✅ Фильтр применяется везде: `monthStats`, `weekStats`, `evOnDay`, `renderOverdue`, `renderSumBar`, `openPendingDocs`
- ✅ `renderContracts()` показывает только `isContract(inv) === true`

### 3.7 Приоритеты счетов ✅
- ✅ 🔴 Срочно / 🟡 Важно / 🟢 Обычный
- ✅ Выпадающий список в карточке
- ✅ Сортировка по приоритету в долгах

### 3.8 Отметить оплаченным ✅
- ✅ `✓ Оплачен` в карточке долга
- ✅ Отмена оплаты (`↩ Отменить`)
- ✅ Визуальное зачёркивание в календаре

---

## ФАЗА 4 — PDF и Google Drive (04.04.2026)

### 4.1 PDF из вложений ✅
- ✅ Скачивание PDF вложений при сканировании
- ✅ Парсинг текста из PDF (`pypdf`)
- ✅ Улучшение точности извлечения суммы из PDF

### 4.2 PDF иконка ✅
- ✅ SVG иконка PDF — `pdfIcon(size)` JS функция
- ✅ В ячейках календаря, карточках долгов, предложениях, боковой панели
- ✅ Клик → открывает PDF напрямую

### 4.3 Google Drive ✅
- ✅ Google OAuth авторизация (Drive + Gmail одним токеном)
- ✅ Автозагрузка PDF при сканировании
- ✅ Структура: `PayCalendar/Счета/2026/04.April/` и `PayCalendar/Предложения/2026/`
- ✅ `☁️` иконка Drive рядом с PDF
- ✅ Google Drive = PRIMARY storage (Railway Volume только для ссылок)

### 4.4 Google Drive статус ✅
- ✅ Polling статуса после OAuth (каждые 3 сек)
- ✅ `postMessage` из popup → основное окно
- ✅ `✅ Подключено: email@gmail.com` в UI

---

## ФАЗА 5 — Gmail и поиск (04–05.04.2026)

### 5.1 Gmail интеграция ✅
- ✅ `GMAIL_CUTOFF = "2026-04-05"` — граница Zone/Gmail
- ✅ Zone IMAP: все письма ДО cutoff
- ✅ Gmail API: все письма С cutoff
- ✅ Один OAuth токен для Drive и Gmail

### 5.2 Поиск ✅
- ✅ Поле ввода прямо в топбаре (не кнопка-иконка)
- ✅ Живой поиск при наборе (debounce 200ms)
- ✅ Дропдаун с результатами
- ✅ Клик → открывает PDF или переходит в календарь
- ✅ `/api/search?q=&type=&n=` API endpoint

### 5.3 Защита паролем ✅
- ✅ `/keys` требует cookie `pc_token=pay123`
- ✅ `/rescan`, `/debug-log` требуют авторизации
- ✅ `/api/*` возвращает 401 без токена
- ✅ Редирект на `/login-page?next=...`

---

## ФАЗА 6 — Synology и настройки (05.04.2026)

### 6.1 Страница настроек /keys ✅
- ✅ Раздел AI ключей: Gemini / Groq / OpenAI / Claude
- ✅ Live статус пулов
- ✅ Сохранение в `/data/config.ini`
- ✅ Настройки почты: IMAP хост/порт/папка
- ✅ Добавить / удалить / тест соединения

### 6.2 Synology WebDAV ✅
- ✅ WebDAV интеграция с Synology NAS
- ✅ Листинг папок, поиск файлов, скачивание через proxy
- ✅ Структура: `/QualityDesk/Сертификаты/`, `/Чертежи/`, `/НД/`
- ✅ Настройка на /keys с тест-кнопкой

---

## ФАЗА 7 — Надёжность и UX (05.04.2026)

### 7.1 Независимость скана от деплоев 🔧
**Запрос:** Сканирование не должно прерываться при деплое или перезапуске сервера.

- ✅ `PENDING_FILE = /data/scan_pending.json`
- ✅ `_save_pending_scan(from_date, to_date, quick)` — пишет задание на диск перед стартом
- ✅ `_clear_pending_scan()` — удаляет файл при успешном завершении или остановке
- ✅ `_start_background()` при старте: читает PENDING_FILE, через 5 сек возобновляет скан
- ✅ Данные выживают редеплой (Railway Volume)
- 🔧 Требует проверки: файл был дважды потерян при rebase, восстановлен

### 7.2 Видимость прогресса сканирования 🔧
**Запрос:** Показывать что происходит во время фазы загрузки заголовков (15–20 мин молчания).

- ✅ Панель скана: `position:fixed; bottom:0` (работает с `overflow:hidden` на body)
- ✅ Фаза загрузки заголовков (pct=0): анимированная пульсирующая полоска + «Загрузка заголовков...» + последнее лог-сообщение
- ✅ Фаза анализа (pct>0): реальный % + `done/total · скорость · ETA`
- ✅ `_checkResumePolling()` — автоматически показывает панель если скан идёт при открытии страницы
- ✅ Кнопка ■ Стоп всегда доступна
- 🔧 Требует проверки в продакшене: было `position:sticky` → заменено на `fixed`

### 7.3 Убрать 🔁 кнопку из топбара ✅
**Запрос:** Кнопка слишком опасна в основном интерфейсе.

- ✅ Кнопка убрана из топбара главной страницы
- ✅ Остаётся только на странице `/keys`
- ✅ В мобильном меню (scan sheet) кнопка сохранена

### 7.4 Тултипы на всех кнопках ✅
**Запрос:** При наведении на любой элемент показывать справку.

- ✅ Кастомный JS-движок тултипов (div `#tip-box`, появляется через 420мс)
- ✅ Стилизованный тёмный попап с рамкой
- ✅ Все кнопки топбара имеют `title=`: Месяц, Неделя, Долги, Офферы, Контракты, Этот месяц, +Пред, +3 мес, 🔄

### 7.5 Пересланные письма (Fwd:) ✅
**Требование:** Fwd:-письма не создавать как новые счета, добавлять уведомление 🔔 к оригиналу.

- ✅ Детект в Phase1: `re.match(r'^(fwd?:|fw:)', subj, re.IGNORECASE)`
- ✅ Инкремент `reminder_count` у оригинального счёта
- ✅ Пересланное письмо пропускается (не попадает в AI)
- ✅ В карточке: `🔔 ×N` показывает количество напоминаний

### 7.6 Резервное копирование ✅
**Запрос:** Сохранять данные клиента перед каждым деплоем. Экспорт и импорт в интерфейсе.

- ✅ `GET /api/backup/export` → JSON с `invoices + config + scan_state`
- ✅ `POST /api/backup/import` → режим `merge` или `replace`
- ✅ Страница `/keys` → карточка «💾 Резервное копирование» с кнопками Экспорт / Импорт
- ✅ `deploy.sh` — автоматически скачивает бэкап с production перед `git push`
- ✅ Хранит последние 20 бэкапов в `backups/` (в `.gitignore`)

### 7.7 Локальное тестирование ✅
**Запрос:** Тестировать новый функционал локально без деплоя на основной сервер клиента.

- ✅ `python app.py` → `localhost:5000`
- ✅ Данные: локальная папка `data/` (не `/data/` как на Railway)
- ✅ Перед деплоем: `bash deploy.sh "описание"` — бэкап + коммит + пуш

---

## ФАЗА 8 — Баги (найдены в ходе аудита)

### 8.1 `dc.date` вместо `date` в `dayHTML()` ✅
- ✅ Клик по чипу счёта в календаре падал с JS-ошибкой. Исправлено: `dc` не в scope, параметр функции — `date`.

### 8.2 `markPaidOv()` не определена ✅
- ✅ Кнопка ✓ Оплачен в карточках просрочено не работала. Добавлена функция с обновлением состояния без перезагрузки.

### 8.3 Панель скана не отображалась ✅
- ✅ `position:sticky` не работает при `overflow:hidden` на body. Заменено на `position:fixed`.

### 8.4 PENDING_FILE потерян при rebase ✅
- ✅ Восстановлен вместе с `_save_pending_scan`, `_clear_pending_scan`, auto-resume.

### 8.5 Scan already running — главный баг ✅
- ✅ `scan_email()` не захватывает lock — только `_run_bg_scan()`.

### 8.6 UnboundLocalError: ds/subj ✅
- ✅ Полная переработка loop в `process_emails()`.

### 8.7 pdf_text not defined ✅
- ✅ `pdf_text = ""` в начале функции `analyze()`.

### 8.8 Race condition в start_bg_scan ✅
- ✅ `running=True` устанавливается ДО запуска потока внутри lock.
- ✅ Reload from disk before save, `truly_new` = только новые ID.

### 8.9 Дублирующийся route /api/gmail/status ✅
- ✅ Удалён дубликат (AssertionError при старте).

### 8.10 extract_amount("1,800.00") ✅
- ✅ Возвращало 1.8 вместо 1800. Исправлена обработка тысячных разделителей (EU и US форматы).

### 8.11 Ячейки календаря невидимые ✅
- ✅ Убраны конфликтующие override стили, базовый `min-height:110px` сохранён.

### 8.12 has_pdf = false для старых счетов ✅
- ✅ `/api/invoices` физически проверяет диск при каждом запросе.

---

## АРХИТЕКТУРА

```
welltechnology.eu почта (IMAP zone.eu)
         ↓ до 05.04.2026

fswer176@gmail.com (пересылка)
         ↓ с 05.04.2026 Gmail API

PayCalendar Flask App (Railway)
  ├── Сканирование писем → AI анализ → найден счёт
  ├── PDF вложение → Google Drive → PayCalendar/Счета/2026/
  ├── Счета сохраняются в /data/invoices.json (Railway Volume)
  └── Веб-интерфейс (index.html)

Synology NAS (WebDAV)
  └── Документы читаются по запросу

Google Drive
  └── Все PDF счетов и предложений (PRIMARY)
```

**Файлы на Railway Volume (`/data/`):**

| Файл | Содержимое |
|------|-----------|
| `invoices.json` | Все счета |
| `config.ini` | Настройки (email, AI ключи, тема, Drive) |
| `scan_state.json` | Состояние последнего скана |
| `scan_pending.json` | Активное задание скана (для auto-resume) |
| `scan_progress.json` | Прогресс текущего скана |
| `pdfs/` | Локальные PDF файлы |

---

## СТРУКТУРА ДАННЫХ

### Счёт (invoice)
```json
{
  "id": "uid_string",
  "vendor": "Название компании",
  "description": "Текст из письма",
  "amount": 1234.56,
  "currency": "EUR",
  "due_date": "2026-04-15",
  "issue_date": "2026-03-20",
  "status": "pending | paid | overdue",
  "priority": "none | low | medium | high",
  "category": "services | utilities | rent | ...",
  "invoice_number": "INV-001",
  "has_pdf": true,
  "gdrive_link": "https://...",
  "is_contract": false,
  "is_offer": false,
  "reminder_count": 1,
  "vat_rate": 24,
  "vat_amount": 246.91,
  "amount_ex_vat": 1028.45
}
```

---

## API ЭНДПОИНТЫ

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/invoices` | Список всех счетов |
| POST | `/api/invoices` | Сохранить список счетов |
| DELETE | `/api/invoices/<id>` | Удалить счёт |
| POST | `/api/invoices/<id>/paid` | Отметить оплаченным |
| GET | `/api/scan-status` | Статус текущего скана |
| POST | `/api/scan-start` | Запустить скан в фоне |
| POST | `/api/scan-stop` | Остановить скан |
| GET | `/api/config` | Получить конфигурацию |
| POST | `/api/save-config` | Сохранить параметр конфига |
| GET | `/api/backup/export` | Экспорт всех данных (JSON) |
| POST | `/api/backup/import` | Импорт данных из JSON |
| GET | `/api/keys` | Статус AI ключей |
| POST | `/api/keys/add` | Добавить AI ключ |
| GET | `/api/email-accounts` | Список email аккаунтов |
| POST | `/api/email-accounts/add` | Добавить email аккаунт |
| DELETE | `/api/email-accounts/<id>` | Удалить email аккаунт |
| GET | `/api/invoices/cleanup` | Очистить нулевые счета |
| POST | `/api/reset-scan` | Сбросить состояние скана |
| GET | `/api/search` | Поиск по счетам |
| GET | `/api/synology/browse` | Просмотр папок Synology |
| GET | `/api/synology/search` | Поиск файлов Synology |
| GET | `/api/synology/file` | Скачать файл Synology |
| GET | `/api/invoice/<id>/pdf` | Скачать PDF через Drive API |
| GET | `/api/gmail/status` | Статус Gmail OAuth |

---

## ТЕХНИЧЕСКИЕ ДАННЫЕ

**Репозиторий:** github.com/alexkitpl1/paycalendar  
**Платформа:** Railway (автодеплой из main)  
**Размер:** app.py ~4400 строк, index.html ~2300 строк  
**Маршрутов API:** 54  
**Стоимость:** ~$5/мес (Railway app + минимальный Volume)  
**Доступ:** https://paycalendar-production.up.railway.app / пароль pay123

---

## ЧТО НЕ РЕАЛИЗОВАНО (задачи на будущее)

| Задача | Приоритет |
|--------|-----------|
| ❌ Автоматический скан по расписанию (cron) | Высокий |
| ❌ Редактирование счёта вручную | Средний |
| ❌ Уведомления о просроченных счетах через Telegram | Средний |
| ❌ Загрузка PDF вручную (drag & drop) | Средний |
| ❌ Экспорт в Excel/CSV | Низкий |
| ❌ Вкладка "Почта" — просмотр Gmail писем в интерфейсе | Низкий |
| ❌ Bitrix24 синхронизация | Низкий |
| ❌ Полноэкранный поисковый модал | Низкий |
